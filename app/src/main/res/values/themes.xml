<resources>
    <!-- Base application theme. -->
    <style name="Theme.Default" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_background</item>
        <item name="colorPrimaryVariant">@color/light_background</item>
        <item name="colorOnPrimary">@color/dark_background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/dark_background</item>
        <item name="colorSecondaryVariant">@color/dark_background</item>
        <item name="colorOnSecondary">@color/light_background</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/translucent_black</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/dark_background</item>
        <item name="searchViewStyle">@style/Theme.Default.SearchView</item>
        <item name="recyclerViewStyle">@style/Theme.Default.BottomSheet</item>
        <item name="actionBarStyle">@style/Theme.Default.ActionBar</item>
    </style>

    <style name="Theme.Default.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/light_background</item>
        <item name="elevation">0dp</item>
    </style>

    <style name="Theme.Default.BottomSheet" parent="Theme.Default">
        <item name="android:background">@color/light_background</item>
    </style>

    <style name="Theme.Default.SearchView" parent="Widget.AppCompat.Light.SearchView">
        <item name="android:background">@color/light_background</item>
    </style>

    <style name="Theme.Default.NotificationIndicator" parent="Theme.Default">
        <item name="android:tint">@android:color/black</item>
        <item name="android:tintMode">src_atop</item>
    </style>

    <!-- Gruvbox Theme -->
    <style name="Theme.Gruvbox" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_bg_gruvbox</item>
        <item name="colorPrimaryVariant">@color/light_bg_gruvbox</item>
        <item name="colorOnPrimary">@color/light_text_gruvbox</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/light_accent_gruvbox</item>
        <item name="colorSecondaryVariant">@color/light_accent_gruvbox</item>
        <item name="colorOnSecondary">@color/light_bg_gruvbox</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/translucent_black</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/light_text_gruvbox</item>
        <item name="android:textColorPrimary">@color/light_text_gruvbox</item>
        <item name="android:textColorSecondary">@color/light_text_secondary_gruvbox</item>
        <item name="bottomSheetStyle">@style/Theme.Gruvbox.BottomSheet</item>
        <item name="searchViewStyle">@style/Theme.Gruvbox.SearchView</item>
        <item name="recyclerViewStyle">@style/Theme.Gruvbox.BottomSheet</item>
    </style>

    <style name="Theme.Gruvbox.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/light_bg_gruvbox</item>
        <item name="elevation">0dp</item>
    </style>

    <style name="Theme.Gruvbox.BottomSheet" parent="Theme.Default">
        <item name="android:background">@color/light_bg_gruvbox</item>
    </style>

    <style name="Theme.Gruvbox.SearchView" parent="Widget.AppCompat.Light.SearchView">
        <item name="android:background">@color/light_bg_gruvbox</item>
        <item name="iconTint">@color/light_text_gruvbox</item>
    </style>
</resources>